import 'package:flutter/material.dart';

import 'outlet_info.dart';

class TopUi extends StatelessWidget {
  const TopUi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const OutletInfo();
  }
}